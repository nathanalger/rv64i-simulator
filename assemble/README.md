# Assembler

Please note this is in no way a good assembler. I had ChatGPT write this for me as I am not the most proficient in python. It works for these purposes though.

# Use

Compile a file like so:

```bash
python assembler.py [input file] [output file]
```

example:

```bash
python assembler.py test.asm test.bin
```